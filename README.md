# Server
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5633

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lisah_db
DB_USER=postgres
DB_PASSWORD=Suavage12.

# Email (SMTP) - Add when you get one sha
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@lisah.com

# CORS - Add the frontend URL when deploying sha
# CORS_ORIGIN=https://yourdomain.com